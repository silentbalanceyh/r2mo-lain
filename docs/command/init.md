# mxt init

## 用途

初始化 R2MO 规范目录结构

## 参数

| 参数 | 说明 | 类型 |
|:---|:---|:---|
| `-d` / `--dir` | 目标目录（默认为当前目录） | string |

## 说明

- 可先运行 `mxt help -c init` 查看 CLI 内置帮助。

## 命令执行记录

```bash
mxt init
mxt init -d .
```
