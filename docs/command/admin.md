# mxt admin

## 用途

根据项目需求文档生成前端页面结构

## 参数

| 参数 | 说明 | 类型 |
|:---|:---|:---|
| `-d` / `--dir` | 目标目录（默认为当前目录） | string |

## 说明

- 可先运行 `mxt help -c admin` 查看 CLI 内置帮助。

## 命令执行记录

```bash
mxt admin
mxt admin -d .
```
